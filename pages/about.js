import style from "../styles/about.module.scss";

const about = () => {
  // const cards = document.querySelectorAll(".card");
  // const observer = new IntersectionObserver((entries) => {
  //   console.log(entries);
  // });

  // observer.observer(cards[0]);
  return (
    <div></div>
    // <div className={style.cardContainer}>
    //   <div className={`{style.card}`}>This is a First Card</div>
    //   <div className={`{style.card}`}>This is a card</div>
    //   <div className={`{style.card}`}>This is a card</div>
    //   <div className={`{style.card}`}>This is a card</div>
    //   <div className={`{style.card}`}>This is a card</div>
    //   <div className={`{style.card}`}>This is a card</div>
    //   <div className={`{style.card}`}>This is a card</div>
    //   <div className={`{style.card}`}>This is a card</div>
    //   <div className={`{style.card}`}>This is a card</div>
    //   <div className={`{style.card}`}>This is a card</div>
    //   <div className={`{style.card}`}>This is a card</div>
    //   <div className={`{style.card}`}>This is a card</div>
    //   <div className={`{style.card}`}>This is aLast Card</div>
    // </div>
  );
};

export default about;
